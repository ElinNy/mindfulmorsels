import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  servingFilterContainer: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    marginBottom: 16,
    marginLeft: 10,
  },

  servingFilterLabel: {
    fontSize: 14,
    fontFamily: "Poppins-Regular",
    marginRight: 10,
    color: "#333",
  },

  servingSlider: {
    width: 100,
    height: 30,
  },
});
